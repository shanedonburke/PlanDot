import { SwipeRevealDirective } from './swipe-reveal.directive';

describe('SwipeRevealDirective', () => {
  it('should create an instance', () => {
    // const directive = new SwipeRevealDirective();
    // expect(directive).toBeTruthy();
  });
});
